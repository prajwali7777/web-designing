//slice map and  filter in this
let nos = [1, 2, 3, 4, 5, 6, 7, 8];
console.log(nos);
console.log(nos.slice(2,7));
nos.map(
    function(n)
     {
        console.log(n*2);
     }
    )
nos.filter(
    function(n)
)